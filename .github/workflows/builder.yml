name: Build SF8008 Kernel

on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-22.04

    env:
      ARCH: arm
      CROSS_COMPILE: arm-linux-gnueabihf-

    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4

      - name: Set up toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc-arm-linux-gnueabihf \
            make \
            bc \
            libncurses-dev \
            bison \
            flex \
            libssl-dev \
            cpio \
            u-boot-tools \
            device-tree-compiler \
            build-essential

      - name: Copy sf8008 config
        run: |
          wget https://raw.githubusercontent.com/jinglingxiaoguin/sf8008-hi3136-build/blob/main/sf8008.config -O .config

      - name: Start kernel build
        run: |
          make olddefconfig
          make -j$(nproc)

      - name: Pack build output
        run: |
          mkdir -p output
          cp arch/arm/boot/zImage output/
          cp arch/arm/boot/dts/*.dtb output/ || true
          find . -name "*.ko" -exec cp {} output/ \;

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sf8008-kernel-output
          path: output
